# Gate 1d (scripts/check-phase-boundary.sh) — trích dẫn cố ý, đã có lý do trong
# chính tài liệu. Mỗi mục: một dòng # lý do, rồi một dòng chuỗi con cần khớp.

# docs/product/1-system-design/architecture.md §12.3 tự khai "cố ý vượt ranh
# giới §8 đặt ra... vì chủ repo yêu cầu thẳng một mục DB cho phần nợ. Nó là đề
# xuất gửi sang pha 2, không phải lược đồ đã chốt" (dòng ngay trên đoạn API).
# Phát hiện ở P1-09 (2026-09-07): Gate 1d quét TOÀN FILE khi architecture.md đổi,
# không chỉ phần vừa sửa, nên bắt luôn trích dẫn có sẵn từ trước ADR-035.
/api/v1` đang có
