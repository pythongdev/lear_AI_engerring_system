# Scope của task đang làm — dùng bởi scripts/check-scope.sh (Gate 3).
#
# Khai báo trước khi bắt đầu task, khớp với mục Scope trong prompt.
# Mỗi dòng một pattern:
#
#   order/         cho phép mọi thứ dưới order/
#   docs/x.md      cho phép đúng file này
#   !order/db.go   cấm, kể cả khi một dòng allow ở trên đã khớp
#
# Lưu ý: * khớp cả dấu / (order/* cũng khớp order/sub/a.go).
#
# File này rỗng (chỉ có comment) = chưa khai báo scope → Gate 3 bỏ qua.
# Xoá hết pattern sau khi task xong.
#
# ⚠️ BẢN ĐÃ COMMIT (HEAD) CỦA FILE NÀY CHỈ ĐƯỢC CHỨA COMMENT (CLAUDE.md §6,
# work/findings.md F-020, docs/decisions.md ADR-043). Pattern là trạng thái của
# phiên đang chạy — không bao giờ đi vào git. Gate 3 (scripts/check-scope.sh) và
# Gate 7b (scripts/check-commit-block.sh) cùng thi hành hình bất biến này: HEAD
# còn pattern mà cây làm việc vẫn giữ ⇒ Gate 3 đỏ; khối commit đưa file này vào
# mà nội dung còn pattern ⇒ Gate 7b kêu. THÊM khối của bạn ở dưới khi bắt một
# task, và GỠ nó trước khi giao khối commit cuối cùng của task đó (§7.3) — đừng
# ghi đè khối của phiên khác đang chạy song song (work/findings.md F-010, F-014).
#
# Lịch sử: T-047 (2026-09-07) đóng work/findings.md F-020 — ba khối pattern của
# BA-04/T-027/T-031 đã nằm trong git từ commit 12c77f8 (2026-08-31) mà không ai
# gỡ được, vì gỡ tạo một thay đổi tracked buộc phải commit — đúng cái luật cũ
# cấm. Đường 2 (ADR-043) sửa hai script ở trên để hình bất biến có cổng gác, rồi
# mới dọn sạch file này. Đọc `work/findings.md` F-020 nếu cần biết toàn bộ diễn
# biến.
#
# 2026-09-16 (T-078): dọn nốt MÓN NỢ TRẠNG THÁI SCOPE lần thứ hai. Bản HEAD của
# file này mang 73 pattern của những task đã xong (P1-12, T-077, và các khối
# trước nữa), nên Gate 3 đỏ ở MỌI lượt, không riêng lượt nào. Cây làm việc nay
# comment-only và file đi kèm khối commit của T-078 — đúng đường CLAUDE.md §6.1
# mô tả cho ca này. Phiên nào đang chạy song song mà cần scope thì THÊM khối mới
# xuống dưới đây.

# ==== T-081 (2026-09-20) — sổ mô tả pha 2: work/backlog_DB.md ====
# (khối này bị một phiên song song xoá mất một lần lúc 2026-09-20 — F-010 · F-014.
#  Phiên nào dọn scope: xoá ĐÚNG khối của mình, không xoá cả file.)
CLAUDE.md
work/backlog.md
work/backlog_DB.md

# ==== ADM-21 (2026-09-20) — lời `C36` về owner ====
# Khối này GIỮ cho tới khi commit của ADM-21 được tạo: mọi file dưới đây còn là
# thay đổi tracked chưa commit, và gỡ khối sớm làm Gate 3 chấm chúng bằng scope
# của phiên khác (F-010 · F-014). Phiên nào dọn: xoá ĐÚNG khối của mình.
master_plan/shop-facts.md
docs/product/0-ba/admin/01-ranh-gioi.md
docs/product/1-system-design/architecture.md
docs/product/99-unknowns.md
docs/work-flow-session/tom_tat_du_an.md
work/admin-questions.md
work/backlog.md
work/backlog_AD.md
work/findings.md
prompt/AD/README.md
prompt/AD/ADM-21-loi-c36-ve-owner-L2.md
