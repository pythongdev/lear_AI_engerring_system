# Scope của task đang làm — dùng bởi scripts/check-scope.sh (Gate 3).
#
# Khai báo trước khi bắt đầu task, khớp với mục Scope trong prompt.
# Mỗi dòng một pattern:
#
#   order/         cho phép mọi thứ dưới order/
#   docs/x.md      cho phép đúng file này
#   !order/db.go   cấm, kể cả khi một dòng allow ở trên đã khớp
#
# Lưu ý: * khớp cả dấu / (order/* cũng khớp order/sub/a.go).
#
# File này rỗng (chỉ có comment) = chưa khai báo scope → Gate 3 bỏ qua.
# Xoá hết pattern sau khi task xong.
#
# ⚠️ BA PHIÊN ĐANG CHẠY SONG SONG TRONG CÙNG MỘT CÂY (2026-08-31): BA-04, T-027,
# T-031. Một file, ba chủ — phiên vào sau THÊM khối của mình, KHÔNG ghi đè
# (work/backlog.md → Ready, bài học của T-019/T-023; work/findings.md F-010).

# --- BA-04 — XONG 2026-08-31, nhưng CHƯA COMMIT nên khối này còn phải ở lại --
# Ba file dưới đây vẫn là thay đổi tracked trong cây; gỡ sớm là Gate 3 đỏ.
# Khối dòng lấy nguyên từ mục Scope của prompt/BA/03-slice-ship-pickup-L2.md.
# GỠ NGAY SAU KHI commit BA-04 (CLAUDE.md §7.3). Phiên khác đừng xoá hộ.
# File dùng chung — BA-04 chỉ sửa MỤC của mình:
#   docs/product.md        chỉ §3.2 (§3.1 của BA-03 đã commit, không chạm)
#   quality/invariants.md  chỉ THÊM I-007, I-008
#   work/backlog.md        chỉ entry BA-04, dòng Ready/In Progress/Done của nó,
#                          và hai dòng §10 câu 6, câu 7 (BA-04 sở hữu hai câu đó)
docs/product.md
quality/invariants.md
work/backlog.md

# --- T-027 — Brief nói ra phần nó đã cắt (F-012) -----------------------------
# Khối này do CHÍNH phiên T-027 viết lại 2026-08-31, thay khối mà phiên BA-04 suy
# ra hộ (`scripts/` là quá rộng: T-027 chỉ chạm hai file trong đó). Cảm ơn phiên
# BA-04 đã dựng lại — chỗ mất là do lỡ ghi đè cả file thay vì thêm một khối.
# GỠ NGAY SAU KHI commit T-027 (CLAUDE.md §7.3). Phiên khác đừng xoá hộ.
# T-027 sở hữu riêng: scripts/brief.sh, scripts/brief.test.sh, và CLAUDE.md §7.1
# (§7.1 liệt kê brief in những gì; thiếu "nói ra phần đã cắt" là nó tả một brief
# không còn tồn tại).
# File dùng chung, mỗi task chỉ sửa MỤC của mình:
#   work/backlog.md   T-027 chỉ entry T-027 và dòng Ready/In Progress/Done của nó
#   work/findings.md  T-027 chỉ mục F-012 (→ Fixed) · T-031 giữ F-013
# KHÔNG chạm: docs/, master_plan/, prompt/, quality/ — task này không đổi dữ kiện.
scripts/brief.sh
scripts/brief.test.sh
work/backlog.md
work/findings.md
CLAUDE.md

# --- T-031 — Bản xuất khẩu bỏ nút `Xong` ở màn trạm (F-013) ------------------
# ⚠️ KHỐI NÀY CŨNG DO PHIÊN BA-04 DỰNG LẠI 2026-08-31 (lý do như trên).
# Suy từ Acceptance của entry T-031: bản xuất khẩu + bốn pointer phải sửa cùng lần.
# Phiên T-031: đọc lại và sửa khối của mình nếu thiếu.
# GỠ NGAY SAU KHI commit T-031 (CLAUDE.md §7.3).
master_plan/prompt-fullstack.md
docs/architecture.md
docs/decisions.md
work/backlog.md
work/findings.md
# Dọn khối T-031 này NGAY SAU khi khối commit của nó được chạy — xoá sớm hơn là
# làm Gate 3 chấm thay đổi chưa commit của T-031 bằng scope của người khác.
